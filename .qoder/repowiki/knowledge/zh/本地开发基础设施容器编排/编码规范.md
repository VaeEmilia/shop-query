- 每个基础设施服务在 compose 中统一声明 `container_name`、`restart: unless-stopped` 及固定端口映射，便于通过容器名或端口直接访问。
- 易失性数据通过命名卷（mysql_data、es_data、qdrant_data）持久化，而非绑定宿主路径。
- 需要自定义镜像的服务（如 Elasticsearch）采用独立 `Dockerfile` 配合 `build:` 指令，将插件等二进制资源与构建逻辑隔离在子目录中。