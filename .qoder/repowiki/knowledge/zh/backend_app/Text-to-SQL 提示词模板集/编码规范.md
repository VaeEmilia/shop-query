- 每个 prompt 以【角色】开头定义专家人设，随后按【上下文信息】→【任务要求】→输入示例的结构组织内容。
- 上下文通过 `{table_infos}`、`{metric_infos}`、`{date_info}`、`{db_info}`、`{query}`、`{sql}`、`{error}` 等花括号占位符注入，由调用方在运行时替换。
- 输出格式强制约束为纯文本或 JSON 数组，并明确禁止 Markdown 代码块、解释性文字或多条 SQL。
- SQL 相关模板严格限定为只读查询，禁止 INSERT/UPDATE/DELETE/CREATE 等写操作。
- 召回类 prompt（column/metric/value）均要求输出 JSON 数组且元素使用中文业务语义，保持最小集合原则。